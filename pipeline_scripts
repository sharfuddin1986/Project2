pipeline {
    agent none
    environment {
        DOCKERHUB_CREDENTIALS=credentials('jenkins docker id')
    }
    stages {
        stage('hello')
          steps {
               echo 'hello world'
               }
           }
     }
  }   
